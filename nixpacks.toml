[phases.setup]
nixPkgs = ["nodejs_22", "npm-9_x"]

[phases.install]
cmds = ["cd backend && npm ci"]

[phases.build]
cmds = ["echo 'No build step required for Node.js server'"]

[start]
cmd = "cd backend && npm start"
